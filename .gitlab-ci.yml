deploy_school_vm:
  stage: deploy
  tags:
    - deploy  # Matches your new runner
  script:
    - cd ~/FestivalAJouer
    - git pull origin main
    - docker compose -f docker-compose.prod.yml up --build -d
  only:
    - main
